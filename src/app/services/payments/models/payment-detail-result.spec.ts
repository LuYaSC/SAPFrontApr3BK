import { PaymentDetailResult } from './payment-detail-result';

describe('PaymentDetailResult', () => {
  it('should create an instance', () => {
    expect(new PaymentDetailResult()).toBeTruthy();
  });
});
