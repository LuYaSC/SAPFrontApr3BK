import { CollaboratorsResult } from './collaborators-result';

describe('CollaboratorsResult', () => {
  it('should create an instance', () => {
    expect(new CollaboratorsResult()).toBeTruthy();
  });
});
