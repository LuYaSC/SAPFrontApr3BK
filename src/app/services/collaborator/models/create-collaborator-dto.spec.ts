import { CreateCollaboratorDto } from './create-collaborator-dto';

describe('CreateCollaboratorDto', () => {
  it('should create an instance', () => {
    expect(new CreateCollaboratorDto()).toBeTruthy();
  });
});
