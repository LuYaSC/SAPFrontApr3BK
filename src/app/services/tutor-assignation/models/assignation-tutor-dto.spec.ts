import { AssignationTutorDto } from './assignation-tutor-dto';

describe('AssignationTutorDto', () => {
  it('should create an instance', () => {
    expect(new AssignationTutorDto()).toBeTruthy();
  });
});
