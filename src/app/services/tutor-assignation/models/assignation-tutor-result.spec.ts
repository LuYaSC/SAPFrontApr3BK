import { AssignationTutorResult } from './assignation-tutor-result';

describe('AssignationTutorResult', () => {
  it('should create an instance', () => {
    expect(new AssignationTutorResult()).toBeTruthy();
  });
});
