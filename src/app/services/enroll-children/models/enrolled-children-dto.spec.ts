import { EnrolledChildrenDto } from './enrolled-children-dto';

describe('EnrolledChildrenDto', () => {
  it('should create an instance', () => {
    expect(new EnrolledChildrenDto()).toBeTruthy();
  });
});
