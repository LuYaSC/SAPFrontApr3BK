import { EnrolledChildrenResult } from './enrolled-children-result';

describe('EnrolledChildrenResult', () => {
  it('should create an instance', () => {
    expect(new EnrolledChildrenResult()).toBeTruthy();
  });
});
