import { AmountBsDirective } from './amount-bs.directive';

describe('AmountBsDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountBsDirective();
    expect(directive).toBeTruthy();
  });
});
