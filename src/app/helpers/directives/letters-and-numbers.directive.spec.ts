import { LettersAndNumbersDirective } from './letters-and-numbers.directive';

describe('LettersAndNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersAndNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
